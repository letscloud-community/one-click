#!/bin/bash

set -euo pipefail

if command -v flock >/dev/null 2>&1; then
  exec 200>/var/lock/clawdbot-boot.lock
  flock -n 200 || exit 0
fi

/usr/local/bin/clawdbot-setup || true

rm -f /var/lib/cloud/scripts/per-instance/001_clawdbot_boot
